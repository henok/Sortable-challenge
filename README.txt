This is my answer to sortable's challenge.

To run my solution, simply clone the repository and type "python matcher.py".

To run with different data, simply replace the two data files located in the same directory with new data files.